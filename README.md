# myqlm-phasespace

This module implements the emulation of noiseless spin dynamics with two mean-field methods:
- a cheap, deterministic approach;
- a more expensive, but also more accurate, stochastic approach based on random trajectories.
